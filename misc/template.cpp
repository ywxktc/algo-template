#include <bits/stdc++.h>
using namespace std;

#ifdef LOCAL
#include "algo/debug.h"
#else
#define debug(...) 42
#endif

using i64 = long long;
using u64 = unsigned long long;
using u32 = unsigned;

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(0);

  return 0;
}
